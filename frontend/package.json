{
  "name": "corvid",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.2.18",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "test:e2e": "playwright test -c tests/playwright.config.ts",
    "test:e2e:report": "playwright show-report -c tests/playwright.config.ts"
  },
  "dependencies": {
    "svelte": "^4.2.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "vite": "^5.3.1",
    "@playwright/test": "^1.47.2"
  }
}
